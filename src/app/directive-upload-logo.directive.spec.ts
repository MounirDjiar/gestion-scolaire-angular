import { DirectiveUploadLogoDirective } from './directive-upload-logo.directive';

describe('DirectiveUploadLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveUploadLogoDirective();
    expect(directive).toBeTruthy();
  });
});
